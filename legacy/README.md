# Archived pre-native-agent implementation

These files are migration references, not a supported execution path. Production
MCP, Ruby and UI must never import or invoke them. Their historical tests describe
the removed provider/chat architecture and are not acceptance tests for the new host.

Use `agent_host/` for runtime integration and `docs/architecture/` for acceptance.
Remove this archive only after the complete live acceptance checklist is signed off.
