[project]
name = "ai-dg-estimator"
version = "0.3.0a0"
description = "AI-dg workspace-first drawing understanding, orthographic reconstruction, reconciliation and deterministic takeoff helpers"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
runtime = [
  "PyMuPDF>=1.24,<2",
  "openpyxl>=3.1,<4",
  "jsonschema>=4.23,<5"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
