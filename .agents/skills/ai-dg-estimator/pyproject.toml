[project]
name = "ai-dg-estimator"
version = "0.2.1a0"
description = "AI-dg drawing understanding, orthographic reconstruction, reconciliation and deterministic takeoff helpers"
requires-python = ">=3.10"
dependencies = [
  "PyMuPDF>=1.24,<2",
  "openpyxl>=3.1,<4",
  "jsonschema>=4.23,<5"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
