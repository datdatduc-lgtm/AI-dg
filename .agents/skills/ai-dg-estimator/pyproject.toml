[project]
name = "ai-dg-estimator"
version = "0.1.0"
description = "Deterministic PDF takeoff helpers for the AI-dg Agent Skill"
requires-python = ">=3.10"
dependencies = [
  "PyMuPDF>=1.24,<2",
  "openpyxl>=3.1,<4",
  "jsonschema>=4.23,<5"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
